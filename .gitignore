/data/
*.db

# SPA build output (Docker/CI builds it; placeholder.html is emitted from
# web/app/public so the embed always has content)
web/app/dist/*
!web/app/dist/placeholder.html
web/app/node_modules/
*.db-shm
*.db-wal
.env
/vmt
/tmp/

# OS / editor / local junk
.DS_Store
**/.DS_Store
.claude/settings.local.json

# Local handoff / pickup notes (obfuscated secrets, internal infra)
handoff.md
web/app/tsconfig.tsbuildinfo
